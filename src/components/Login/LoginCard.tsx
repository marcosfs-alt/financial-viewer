import { Box, Grid2 } from '@mui/material';

const LoginCard = () => {
  return (
    <Grid2 container spacing={2} size={{ xs: 10, md: 8, lg: 6 }}>
      <Box></Box>
    </Grid2>
  );
};

export default LoginCard;
